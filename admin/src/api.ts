const API=(import.meta as any).env?.VITE_API_URL||'http://localhost:5000';let token=localStorage.getItem('dado_admin_token')||'';export const adminApi={async login(phone:string,password:string){const r=await fetch(`${API}/api/admin/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({phone,password})});if(!r.ok)throw new Error('Неверный логин или пароль');const d=await r.json();token=d.accessToken;localStorage.setItem('dado_admin_token',token);return d;},logout(){token='';localStorage.removeItem('dado_admin_token')},hasToken(){return !!token},async get(path:string){const r=await fetch(`${API}${path}`,{headers:{Authorization:`Bearer ${token}`}});if(r.status===401){this.logout();throw new Error('Сессия завершена')}if(!r.ok)throw new Error('Ошибка API');return r.json();}};
