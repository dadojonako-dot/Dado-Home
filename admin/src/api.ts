const API=(import.meta as any).env?.VITE_API_URL||'http://localhost:5000';let token=localStorage.getItem('dado_admin_token')||'';async function request(path:string,options:RequestInit={}){const r=await fetch(`${API}${path}`,{...options,headers:{'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{}) ,...(options.headers||{})}});if(r.status===401){token='';localStorage.removeItem('dado_admin_token');throw new Error('Сессия завершена')}if(!r.ok){let message='Ошибка API';try{const d=await r.json();if(d?.error)message=d.error}catch{}throw Object.assign(new Error(message),{status:r.status})}return r.status===204?null:r.json()}export const adminApi={async login(phone:string,password:string){const r=await fetch(`${API}/api/admin/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({phone,password})});if(!r.ok)throw new Error('Неверный логин или пароль');const d=await r.json();token=d.accessToken;localStorage.setItem('dado_admin_token',token);return d},async me(){if(!token)return null;try{return await request('/api/admin/me')}catch{return null}},logout(){token='';localStorage.removeItem('dado_admin_token')},hasToken(){return!!token},get:(path:string)=>request(path),post:(path:string,data:any)=>request(path,{method:'POST',body:JSON.stringify(data)}),put:(path:string,data:any)=>request(path,{method:'PUT',body:JSON.stringify(data)}),delete:(path:string)=>request(path,{method:'DELETE'})};

